/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'images.unsplash.com',
          'bazingaweb.s3.amazonaws.com'
        ],
    }
}

module.exports = nextConfig
